"""Test updated FactTable classification."""import sys, os, warningssys.path.insert(0, r"C:\Users\BYU\Desktop\NIKTO\packages\nikto-core\src")warnings.filterwarnings("ignore")from rdkit import RDLoggerRDLogger.logger().setLevel(RDLogger.CRITICAL)from nikto.brain.fact_table import FactTableft = FactTable()test_qs = [    "What is the black hole information paradox?",    "Explain Hawking radiation",    "How does the double-slit experiment work?",    "What is quantum entanglement?",    "What is the Schrodinger equation?",    "Explain Many-Worlds interpretation",    "What is the Standard Model of particle physics?",    "Explain general relativity",    "How does a transformer neural network work?",    "What is backpropagation in neural networks?",    "What is the transformer architecture?",    "Explain the Heisenberg Uncertainty Principle",    "What is spontaneous symmetry breaking?",    "What is quantum field theory?",    "How does backpropagation work?",    "What is the Riemann Hypothesis?",    "What is Godel's incompleteness theorem?",    "What is P vs NP problem?",    "Explain the Chinese room argument",    "What is the paperclip maximizer?",    "How does mRNA vaccine work?",    "What is CRISPR-Cas9?",    "What is the Turing test?",]print(f"Total facts: {ft.fact_count()}")print()hits = 0for q in test_qs:    entry = ft.lookup(q)    if entry:        hits += 1        domain = entry.get("domain", "unknown")        ans = entry["answer"][:80]        print(f"HIT [{domain}] {q[:55]} -> {ans}...")    else:        domain = ft._classify(q)        print(f"MISS [{domain}] {q[:55]}")stats = ft.get_stats()print(f"\nHits: {hits}/{len(test_qs)} = {hits/len(test_qs)*100:.0f}%")print(f"Total facts: {stats['total_facts']} | Domains: {stats['domains_covered']}")