"""Test NICTO brain with quiz questions using AKNOW bridge."""import sys, os, warningssys.path.insert(0, r"C:\Users\BYU\Desktop\NIKTO\packages\nikto-core\src")warnings.filterwarnings("ignore")from rdkit import RDLoggerRDLogger.logger().setLevel(RDLogger.CRITICAL)from nikto.brain.core import NiktoBrainbrain = NiktoBrain()print("NICTO Brain initialized")print(f"AKNOW loaded: {brain.aknow.is_loaded}")# Quiz questions that were previously missedquiz_questions = [    "What is the black hole information paradox?",    "Explain Hawking radiation",    "How does the double-slit experiment work?",    "What is quantum entanglement?",    "What is the Schrodinger equation?",    "Explain Many-Worlds interpretation",    "What is the Standard Model of particle physics?",    "Explain general relativity",    "How does a transformer neural network work?",    "What is backpropagation in neural networks?",]print("\n" + "=" * 60)print("QUIZ TEST: Asking NICTO brain")print("=" * 60)for i, q in enumerate(quiz_questions, 1):    print(f"\n--- Question {i}: {q} ---")    result = brain.process(q)    print(f"  Domain: {result.get('domain', 'N/A')}")    print(f"  Source: {result.get('source', 'N/A')}")    print(f"  Factual: {result.get('factual', False)}")    print(f"  Seed: {result.get('seed', 0)}")    if result.get('answer'):        ans = result['answer'][:300]        print(f"  Answer: {ans}...")    else:        thought = result.get('thought', {}).get('content', 'N/A')[:300]        print(f"  Thought: {thought}...")    print(f"  Truth: {result.get('truth_check', 'N/A')}")print("\n" + "=" * 60)print("QUIZ TEST COMPLETE")print("=" * 60)# Also test AKNOW direct for comparisonprint("\n=== AKNOW ThinkEngine Direct ===")for q in ["What is the black hole information paradox?", "Explain Hawking radiation"]:    print(f"\nQ: {q}")    ans = brain.aknow.think_ask(q)    print(f"A: {ans[:500]}...")