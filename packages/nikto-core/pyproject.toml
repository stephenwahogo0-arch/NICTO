[project]
name = "nikto-core"
version = "0.1.0"
description = "NIKTO Core Agent Runtime"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/nikto"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
